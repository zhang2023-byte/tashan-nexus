# 数据管理快速参考

## 📍 关键信息

### 数据存储位置
```
backend/database.sqlite    # SQLite 数据库文件（216 KB）
backend/.env               # 包含 API 密钥等敏感信息
```

### Git 保护状态 ✅
- ✅ `database.sqlite` 已在 `.gitignore` 中
- ✅ `.env` 已在 `.gitignore` 中
- ✅ 推送到 GitHub 不会上传数据

---

## 🛠️ 快速命令

### 查看数据
```bash
cd backend
node manage-data.js status       # 数据库统计
node manage-data.js list-users   # 用户列表
```

### 清理数据
```bash
node manage-data.js clear-test   # 删除测试用户
node manage-data.js clear-all    # 清空所有数据
```

### 备份/导出
```bash
node manage-data.js backup       # 备份数据库
node manage-data.js export       # 导出为JSON
```

---

## 📦 常见场景

### 1️⃣ 上传到 GitHub
```bash
git add .
git commit -m "更新"
git push
```
**结果**：只有代码上传，数据库留在本地 ✅

### 2️⃣ 清理测试数据
```bash
node manage-data.js clear-test
```
**删除用户**：zhangsan, lisi, wangwu 等测试账号

### 3️⃣ 部署新服务器
```bash
# 服务器上
git clone <your-repo>
cd tashan-nexus/backend
npm install
cp .env.example .env
# 编辑 .env 添加 API keys
node server.js
```
**结果**：自动创建新数据库 ✅

### 4️⃣ 迁移数据到新服务器
```bash
# 本地
node manage-data.js backup

# 上传到服务器
scp database.backup.*.sqlite user@server:/path/to/backend/database.sqlite
```

### 5️⃣ 版本更新（保留数据）
```bash
# 服务器上
node manage-data.js backup    # 先备份
git pull                      # 更新代码
pm2 restart tashan-nexus     # 重启服务
```
**结果**：代码更新，数据保留 ✅

---

## ⚠️ 注意事项

### 永远不要提交到 Git
- ❌ `backend/database.sqlite`
- ❌ `backend/.env`
- ❌ `backend/*.backup.sqlite`

### 生产环境建议
- 📅 每天备份数据库
- 🔒 备份文件加密存储
- ☁️  异地备份（多个位置）

---

## 🆘 紧急恢复

### 误删数据库
```bash
# 如果有备份
ls backend/database.backup.*.sqlite
cp backend/database.backup.*.sqlite backend/database.sqlite
```

### 数据损坏
```bash
# 1. 停止服务器
# 2. 从最近的备份恢复
cp database.backup.2026-01-04.sqlite database.sqlite
# 3. 重启服务器
```

---

## 📞 更多帮助

查看完整文档：
```bash
cat docs/数据管理指南.md
```

查看所有命令：
```bash
node manage-data.js help
```
